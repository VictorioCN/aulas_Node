module.exports = {
    soma (a, b){
        const somado = a+b
        console.log("soma:" + (a+b))
        return somado.toString()
    },
    subtracao (a, b){
        console.log("subtracao:" + (a-b))
    }
};
